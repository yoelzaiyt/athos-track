{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'skipping frontend build for the brgps-sync worker'"
  },
  "deploy": {
    "startCommand": "npm run brgps:sync",
    "restartPolicyType": "NEVER"
  }
}
